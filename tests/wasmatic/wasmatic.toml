# wasmatic toml for tests

log_level = ["info", "wasmatic=debug", "just_to_confirm_test=debug"]

chain = "local"

[chains.local]
chain_id = "slay3r-local"
rpc_endpoint = "http://localhost:26657"
grpc_endpoint = "http://localhost:9090"
gas_price = 0.025

[chains.testnet]
chain_id = "layer-permissionless-3"
rpc_endpoint = "https://rpc.layer-p.net"
grpc_endpoint = "https://grpc.layer-p.net:443"
gas_price = 0.025
gas_denom = "uperm"