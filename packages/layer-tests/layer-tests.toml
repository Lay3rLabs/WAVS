# Run all the tests
# all = true
isolated = "eth-block-interval" 

# Run just one test (cannot be enabled at the same time as all)
# isolated = "eth-permissions" 

# See matrix.rs for all the available options
# You can run groups of tests like:

# [matrix.eth]
# block-interval = true

# [matrix.cosmos]
# echo-data = true
# block-interval = true

# [matrix.crosschain]
# cosmos-to-eth-echo-data = true
# etc.