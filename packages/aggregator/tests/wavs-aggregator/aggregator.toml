# aggregator toml for tests

log_level = ["info", "aggregator=debug", "just_to_confirm_test=debug"]

chain = "local"

[chains.eth.local]
chain_id = "31337"
ws_endpoint = "ws://localhost:8545"
http_endpoint = "http://localhost:8545"
aggregator_endpoint = "http://localhost:8001"
