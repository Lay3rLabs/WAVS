This should eventually replace the public `layer-wasi` crate.

For now, to make development and testing easier, it's a local dependency

The current contents of that are copy/pasted into `wasi.rs` here
