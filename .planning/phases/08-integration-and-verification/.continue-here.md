---
phase: 08-integration-and-verification
task: 5
total_tasks: 6
status: blocked
last_updated: 2026-03-20T19:45:00.000Z
---

<current_state>
BLS operator key registration works (3 operators, all updateOperatorSigningKey calls succeed).
Blocked on: Prague hardfork regression — evm_simple_aggregator WASM returns success=false when
Prague is enabled for the anvil chain. Prague is currently applied to ALL EVM chains whenever
BlsMultiOperator is in the test matrix (handles.rs:76).
</current_state>

<completed_work>

- Task 1: 08-01 BLS test infrastructure — DONE (614c9f4d)
- Task 2: Fix BLS bootstrap panic (48b8405e)
- Task 3: Fix G2 coordinate ordering c1|c0 → c0|c1 (23191da1)
- Task 4: Operator ETH funding + skip ECDSA check for BLS ops (96f52554)
</completed_work>

<remaining_work>

- Task 5 (BLOCKED): Fix Prague hardfork regression
  - evm_simple_aggregator fails: EchoData WASM returns success=false on all 3 operators
  - Root cause: Prague EVM applied to ALL chains when BLS in matrix
  - Fix options:
    A) Spawn a SEPARATE anvil chain for BLS tests (prague chain) vs default chain (cancun)
    B) Investigate if EchoData/SimpleTrigger contract changed under Prague compilation
       (check event signature 0x86eacd23 matches expected decode in echo_data.wasm)
  - First: run isolated BLS test to confirm BLS itself passes:
    Edit layer-tests.toml: mode = { "isolated" = [{ evm = "bls_multi_operator" }] }
- Task 6: Complete 08-02 SUMMARY.md and phase verification
</remaining_work>

<decisions_made>

- BLS flow confirmed working: operators fund themselves, updateOperatorSigningKey succeeds
- BLS quorum set to 2/3, BLS submit contract deploys at 0x58635E44...
- Prague needed only for EIP-2537 precompiles — should be isolated to BLS chain
</decisions_made>

<blockers>

- Prague hardfork breaks evm_simple_aggregator (and likely other secp256k1 tests)
- Need to scope Prague to BLS-only EVM chain, not all chains
</blockers>

<context>
Key file: packages/layer-tests/src/e2e/handles.rs:76 — hardfork_prague set for ALL chains
The fix: spawn a dedicated evm:31338 chain with Prague for BLS tests, keep evm:31337 on default.
Or: investigate if the issue is just EchoData failing to decode the Prague-compiled trigger event.
The event topic 0x86eacd23 should be stable (it's a keccak256 of the function signature,
independent of EVM version). The data encoding is also standard ABI, so Prague shouldn't change it.
This makes the Prague→WASM failure mysterious — may be worth running evm_simple_aggregator
in isolation WITH Prague to confirm it reproduces, then WITHOUT Prague to confirm it's the cause.
</context>

<next_action>
ENVIRONMENT: Run `just start-dev` first to ensure WAVS stack is running, then:

Step 1: cargo test -p layer-tests -- --nocapture 2>&1 | tee logs.txt
  (layer-tests.toml already set to isolated bls_multi_operator)

If test fails with BindFailed: stop any previous WAVS instances (pkill -f wavs-app)
If test fails with "transaction already imported": restart anvil (just start-anvil)

Step 2: Check if BLS passes. If evm_simple_aggregator also fails, run it in isolation:
  mode = { "isolated" = [{ evm = "simple_aggregator" }] }

Step 3: If Prague is confirmed as root cause: spawn dedicated evm:31338 for BLS
  (Prague needed only for EIP-2537 precompiles — scope to BLS chain only)

WASIP2 BUILD: Alloy transport gating committed (0dd71c68) but tokio still
leaks in from unknown dep. The tokio error comes from wasip2 unstable feature
in tcp networking code. Need to find the non-alloy source of tokio for wasm32.
Candidates: layer-climb, cosmwasm-std, or another dep in example-helpers.
Use: cargo tree -p echo-data --target wasm32-wasip2 -i tokio to trace it.
</next_action>
