---
phase: 08-integration-and-verification
task: 3
total_tasks: 5
status: blocked
last_updated: 2026-03-20T17:33:03.223Z
---

<current_state>
Debugging BLS `updateOperatorSigningKey` revert in E2E tests. The `cast send` to call this contract function fails with "Out of gas: gas required exceeds allowance: 0" from `eth_estimateGas`, meaning the transaction reverts on simulation.

Last fix applied: added `--gas 5000000` to the cast send to bypass eth_estimateGas and get the REAL revert reason on next test run.
</current_state>

<completed_work>

- Task 1: Phase 8 plan 08-01 (BLS test infrastructure) — DONE, committed
- Task 2: Phase 8 plan 08-02 partial execution:
  - BLS operator registration infrastructure done
  - Fixed: private module path in helpers.rs
  - Fixed: BLS panic at bootstrap (accept Secp256k1 HD index for BLS key derivation)
  - Fixed: G2 coordinate ordering in `bls_g2_signature_bytes` — blst ZCash format is c1|c0, EIP-2537 expects c0|c1 (committed `23191da1`)
  - Added `--gas 5000000` to `updateOperatorSigningKey` cast send to diagnose revert (uncommitted)
</completed_work>

<remaining_work>

- Task 3 (BLOCKED): Fix `updateOperatorSigningKey` revert — need to run tests with `--gas 5000000` to see actual revert reason (PrecompileCallFailed = Prague not active, vs InvalidBLSKeyOwnershipProof = wrong signature)
- Task 4: Verify secp256k1 regression still works
- Task 5: Complete 08-02 SUMMARY.md, update STATE.md/ROADMAP.md, run phase verifier
</remaining_work>

<decisions_made>

- Confirmed blst G2 serialization order via source: `e2.c` line 183 — `X[1]` (c1/imaginary) goes first, `X[0]` (c0/real) second. My fix is correct.
- Commonware uses `MinPk` variant confirmed — G1 pubkey (48 bytes compressed), G2 signatures
- `hashToCurveG2` in Solidity confirmed correct RFC 9380 implementation
- Added `--gas 5000000` as diagnostic to bypass eth_estimateGas; will show real revert reason
</decisions_made>

<blockers>

- `updateOperatorSigningKey` reverts but we can't distinguish WHY from the "Out of gas" estimateGas error
- After adding `--gas 5000000`, next run will reveal actual revert: PrecompileCallFailed (Prague not working) or InvalidBLSKeyOwnershipProof (bad signature)
</blockers>

<context>
We've verified:
1. blst G2 format: c1|c0 (confirmed from blst/src/e2.c source)
2. EIP-2537 format: c0|c1 (confirmed from spec)
3. G2 fix in bls_signing.rs is correct
4. G1 encoding is correct (min_pk, 48-byte compressed, padded to 128 bytes)
5. Message encoding: keccak256(abi.encode(operator_address)) — matches on both sides
6. Solidity DST matches Rust DST: "BLS_SIG_BLS12381G2_XMD:SHA-256_SSWU_RO_"
7. All 64 Solidity BLS tests pass, 14 Rust unit tests pass

Anvil 1.4.4 with `--hardfork prague` should support EIP-2537. The `--gas 5000000` bypass will tell us definitively.

Key file: packages/utils/src/test_utils/middleware/evm/middleware_poa_bls.rs line ~221 has the `--gas 5000000` diagnostic added
</context>

<next_action>
Run E2E tests: `cargo test -p layer-tests -- --nocapture 2>&1 | tee logs.txt`

Look for what error follows "updateOperatorSigningKey" — it will be either:
- "PrecompileCallFailed" → Prague not enabled properly, investigate anvil hardfork
- "InvalidBLSKeyOwnershipProof" → signature still wrong, need to debug encoding further
- "success" → the `--gas` fix worked, just gas estimation bug
</next_action>
