---
phase: 04-validation-and-cleanup
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - docs/P2P.md
  - docs/ARCHITECTURE.md
  - CLAUDE.md
  - docs/blog/commonware-p2p-migration.md
  - docs/OPERATOR_MIGRATION.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "docs/P2P.md documents the commonware P2P architecture, not the old libp2p architecture"
    - "docs/P2P.md contains config examples for Disabled, Local, and Remote modes"
    - "docs/P2P.md contains multi-node setup instructions"
    - "docs/ARCHITECTURE.md P2P section references commonware, not GossipSub/mDNS/Kademlia"
    - "CLAUDE.md no longer references libp2p for P2P trigger distribution"
    - "A blog post exists in docs/blog/ announcing the commonware integration"
    - "An operator migration guide exists documenting identity change, config format change, and coordinated upgrade"
  artifacts:
    - path: "docs/P2P.md"
      provides: "Complete P2P documentation for commonware backend"
      contains: "commonware"
      min_lines: 80
    - path: "docs/ARCHITECTURE.md"
      provides: "Updated architecture doc with commonware P2P reference"
      contains: "commonware"
    - path: "CLAUDE.md"
      provides: "Updated project instructions without libp2p references"
    - path: "docs/blog/commonware-p2p-migration.md"
      provides: "Announcement blog post"
      min_lines: 40
    - path: "docs/OPERATOR_MIGRATION.md"
      provides: "Operator migration guide"
      contains: "Ed25519"
      min_lines: 60
  key_links:
    - from: "docs/P2P.md"
      to: "wavs.toml"
      via: "config examples reference wavs.toml format"
      pattern: "wavs\\.p2p"
    - from: "docs/OPERATOR_MIGRATION.md"
      to: "docs/P2P.md"
      via: "cross-reference for detailed config docs"
      pattern: "P2P\\.md"
---

<objective>
Rewrite P2P documentation for the commonware backend, update stale references in ARCHITECTURE.md and CLAUDE.md, create a blog post announcing the migration, and write an operator migration guide.

Purpose: Operators need accurate documentation to configure and upgrade their nodes. The old docs describe libp2p/GossipSub/mDNS/Kademlia which no longer exist in the codebase.

Output: Complete documentation suite covering the commonware P2P architecture, configuration, migration steps, and announcement.
</objective>

<execution_context>
@/Users/jacobhartnell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jacobhartnell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-and-cleanup/04-RESEARCH.md

<interfaces>
<!-- Current wavs.toml P2P config section (lines 192-243) for documentation reference -->
```toml
# P2P networking settings (commonware)
# P2P is disabled by default (for single-operator setups).
# Enable for multi-operator deployments to share submissions and reach quorum consensus.
#
# Peer identities use Ed25519 public keys derived from signing_mnemonic.
# Addresses use socket format: "<hex_ed25519_pubkey>@<host>:<port>"

# Option 1 -- Disabled (default, single-operator):
# p2p = "disabled"

# Option 2 -- Local lookup mode (development/testing, known peer addresses):
# [wavs.p2p.local]
# listen_port = 9000
# peer_addresses = ["<hex_ed25519_pubkey>@127.0.0.1:9001"]
# authorized_peers = ["<hex_ed25519_pubkey>"]
# max_message_size = 65536
# deque_size = 128

# Option 3 -- Remote discovery mode (production, bootstrapper-based peer discovery):
# [wavs.p2p.remote]
# listen_port = 9000
# bootstrappers = []
# authorized_peers = ["<hex_ed25519_pubkey>"]
# max_message_size = 65536
# deque_size = 128
```

<!-- P2pStatus struct fields for status endpoint documentation -->
```rust
pub struct P2pStatus {
    pub enabled: bool,
    pub local_peer_id: String,        // hex Ed25519 public key
    pub listen_addresses: Vec<String>, // socket addresses
    pub connected_peers: usize,
    pub peer_ids: Vec<String>,        // hex Ed25519 public keys
    pub subscribed_services: Vec<String>,
}
```

<!-- Key architecture facts from Phases 1-3 -->
- Identity: Ed25519 keypair from WAVS_SIGNING_MNEMONIC via ChaCha20Rng (BIP-39 seed)
- Two modes: Local (lookup, known addresses) and Remote (discovery, bootstrappers)
- Single broadcast channel with application-level service filtering (ServiceRouter)
- Broadcast Engine for message caching and catch-up on reconnection
- Oracle-based peer authorization (only known operators can connect)
- Rate limiting via commonware Config::local() defaults
- Address format: <hex_ed25519_pubkey>@<host>:<port>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/P2P.md and update ARCHITECTURE.md + CLAUDE.md</name>
  <files>
    docs/P2P.md
    docs/ARCHITECTURE.md
    CLAUDE.md
  </files>
  <read_first>
    docs/P2P.md
    docs/ARCHITECTURE.md
    CLAUDE.md
    wavs.toml
    packages/wavs/src/subsystems/aggregator/p2p.rs
  </read_first>
  <action>
    1. **Rewrite `docs/P2P.md`** entirely. Replace the full file with a new document covering the commonware P2P architecture. Structure:

       ```markdown
       # P2P Networking

       [ASCII diagram similar to current one, but replace "mDNS/Kademlia + GossipSub" box
        with "commonware-p2p" and "Broadcast Engine"]

       ## Overview
       P2P networking is an optional feature of the Aggregator subsystem. When enabled,
       operators broadcast signed submissions to peers via a commonware broadcast channel,
       and the Aggregator collects signatures to reach quorum before posting on-chain.

       Uses commonware-p2p for authenticated peer networking with Ed25519 identities.
       A single broadcast channel handles all services, with application-level filtering
       by service ID (ServiceRouter).

       ## Architecture
       - **Identity**: Ed25519 keypair derived from WAVS_SIGNING_MNEMONIC via ChaCha20Rng
       - **Two modes**: Local (lookup with known addresses) and Remote (discovery with bootstrappers)
       - **Broadcast**: Single channel with ServiceRouter filtering by service_id
       - **Catch-up**: Buffered Broadcast Engine caches messages per peer for reconnection recovery
       - **Security**: Oracle-based peer authorization, built-in per-peer rate limiting

       ## Configuration
       P2P is disabled by default. Add a `[wavs.p2p]` block to `wavs.toml` to enable.

       ### Disabled (default)
       [example: p2p = "disabled" or omit entirely]

       ### Local -- Lookup Mode (development/testing)
       [example with listen_port, peer_addresses, authorized_peers, max_message_size, deque_size]

       ### Remote -- Discovery Mode (production)
       [example with listen_port, bootstrappers, authorized_peers, max_message_size, deque_size]

       ## Multi-Node Setup

       ### Local Development (2 operators on localhost)
       Step-by-step with two wavs.toml files, referencing the local dev preset from wavs.toml.
       Include how to get Ed25519 public key: `wavs-cli p2p identity --mnemonic "..."`

       ### Production Deployment
       - Start one node as bootstrapper (empty bootstrappers list)
       - Other nodes list bootstrapper address
       - Exchange Ed25519 public keys for authorized_peers

       ## Status Endpoint
       GET /p2p/status returns JSON with:
       - enabled (bool)
       - local_peer_id (hex Ed25519 public key)
       - listen_addresses (socket addresses, e.g. "0.0.0.0:9000")
       - connected_peers (count)
       - peer_ids (hex Ed25519 public keys of connected peers)
       - subscribed_services (service names)

       ## Identity
       Ed25519 keypair derived via ChaCha20Rng from BIP-39 mnemonic seed.
       Deterministic: same mnemonic always produces same peer ID.
       Peer ID format: hex-encoded Ed25519 public key (64 hex chars).
       Address format: <peer_id>@<host>:<port>
       ```

    2. **Update `docs/ARCHITECTURE.md`** line 80:
       Replace:
       ```
       The Aggregator subsystem includes optional P2P networking for multi-operator deployments. Each registered service gets its own GossipSub topic; operators subscribe on service registration and unsubscribe on removal. Peer discovery uses either mDNS (local/dev) or Kademlia DHT (production). See [P2P.md](P2P.md) for configuration details.
       ```
       With:
       ```
       The Aggregator subsystem includes optional P2P networking for multi-operator deployments. Operators broadcast signed submissions via a commonware broadcast channel with application-level service filtering. Peer connectivity uses either lookup mode (local/dev, known addresses) or discovery mode (production, bootstrapper-based). Identities are Ed25519 keypairs derived from the signing mnemonic. See [P2P.md](P2P.md) for configuration details.
       ```

    3. **Update `CLAUDE.md`**:
       - Line 82: Replace "Uses libp2p for P2P trigger distribution" with "Uses commonware-p2p for P2P message broadcast between operators."
       - Line 137: Replace "P2P.md -- libp2p/Hyperswarm networking" with "P2P.md -- commonware P2P networking"
  </action>
  <verify>
    <automated>cd /Users/jacobhartnell/Dev/WAVS/WAVS && grep -c "commonware" docs/P2P.md && grep -c "GossipSub\|mDNS\|Kademlia\|libp2p" docs/P2P.md | xargs -I{} test {} -eq 0 && grep -c "commonware" docs/ARCHITECTURE.md && echo "P2P docs updated"</automated>
  </verify>
  <acceptance_criteria>
    - docs/P2P.md contains "commonware" (at least 5 occurrences)
    - docs/P2P.md contains "Ed25519"
    - docs/P2P.md contains "## Configuration"
    - docs/P2P.md contains "## Multi-Node Setup"
    - docs/P2P.md contains "## Status Endpoint"
    - docs/P2P.md contains "lookup" (for Local mode)
    - docs/P2P.md contains "discovery" (for Remote mode)
    - docs/P2P.md does NOT contain "GossipSub"
    - docs/P2P.md does NOT contain "mDNS" (as a feature name)
    - docs/P2P.md does NOT contain "Kademlia"
    - docs/P2P.md does NOT contain "multiaddr"
    - docs/P2P.md has at least 80 lines
    - docs/ARCHITECTURE.md line 80 area contains "commonware" and does NOT contain "GossipSub"
    - CLAUDE.md contains "commonware-p2p" and does NOT contain "libp2p" (in the trigger manager description)
    - CLAUDE.md line 137 area contains "commonware P2P" not "libp2p/Hyperswarm"
  </acceptance_criteria>
  <done>docs/P2P.md fully rewritten for commonware architecture, ARCHITECTURE.md P2P section updated, CLAUDE.md stale references replaced</done>
</task>

<task type="auto">
  <name>Task 2: Create blog post and operator migration guide</name>
  <files>
    docs/blog/commonware-p2p-migration.md
    docs/OPERATOR_MIGRATION.md
  </files>
  <read_first>
    docs/P2P.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/04-validation-and-cleanup/04-RESEARCH.md
  </read_first>
  <action>
    1. **Create `docs/blog/` directory** (does not exist yet).

    2. **Create `docs/blog/commonware-p2p-migration.md`** -- Blog post in announcement style (NOT tutorial). Content:

       ```markdown
       # WAVS Migrates to commonware for P2P Networking

       **Date:** 2026-03

       ## Summary
       WAVS has replaced its libp2p-based P2P networking layer with commonware primitives.
       This migration delivers simpler architecture, better performance, and tighter security
       for multi-operator deployments.

       ## Why commonware?
       [2-3 paragraphs: libp2p complexity (13 feature flags, GossipSub mesh tuning,
       secp256k1 identity), commonware's focused design (broadcast Engine, Oracle authorization,
       Ed25519 identity), alignment with WAVS's use case (simple broadcast, not full mesh)]

       ## What Changed
       [Bullet points covering the major changes:]
       - Identity: secp256k1 -> Ed25519 (deterministic from signing mnemonic)
       - Address format: multiaddr -> socket format (<pubkey>@<host>:<port>)
       - Discovery: mDNS/Kademlia DHT -> Lookup/Discovery modes
       - Message routing: Per-service GossipSub topics -> Single broadcast channel with service filtering
       - Catch-up: Custom request/response protocol -> Broadcast Engine with per-peer message caching
       - Security: Connection-level encryption -> Oracle-based peer authorization + rate limiting

       ## Impact on Operators
       [1-2 paragraphs: This is a breaking change requiring coordinated upgrade.
       All operators in a deployment must upgrade simultaneously. Peer IDs change.
       Config format changes. See the operator migration guide for step-by-step instructions.]

       ## Technical Details
       [1-2 paragraphs: Four-phase migration (identity, broadcast, config, validation).
       Zero libp2p code remains. commonware-p2p v2026.3.0 used for networking,
       commonware-broadcast for message dissemination, commonware-cryptography for Ed25519.]

       ## What's Next
       [Brief: Future improvements like on-chain operator registry integration,
       simulated networking tests, NAT traversal guidance.]
       ```

       Tone: Announcement/news style. Concise. Aimed at operators and technical users.
       Do NOT write it as a tutorial or step-by-step guide.

    3. **Create `docs/OPERATOR_MIGRATION.md`** -- Step-by-step migration guide. Content:

       ```markdown
       # Operator Migration Guide: libp2p to commonware

       This guide covers migrating a WAVS operator node from the libp2p P2P backend
       to the new commonware backend.

       ## Prerequisites
       - Current WAVS node running with libp2p P2P
       - Coordination with all operators in your deployment

       ## Breaking Changes

       ### 1. Identity Format Change
       - Old: secp256k1 keypair, peer IDs like `12D3Koo...`
       - New: Ed25519 keypair derived from WAVS_SIGNING_MNEMONIC via ChaCha20Rng
       - Peer IDs are now hex-encoded Ed25519 public keys (64 hex characters)
       - Same mnemonic produces different peer IDs (different key derivation)
       - To view your new peer ID: `wavs-cli p2p identity --mnemonic "your words here"`

       ### 2. Address Format Change
       - Old: multiaddr format `/ip4/1.2.3.4/tcp/9000/p2p/12D3Koo...`
       - New: socket format `<hex_ed25519_pubkey>@<host>:<port>`
       - Example: `a1b2c3d4...@192.168.1.10:9000`

       ### 3. Configuration Format Change
       - Old: `[wavs.p2p]` with `local = { listen_port = 9000 }` (mDNS) or `remote = { listen_port = 9000, bootstrap_nodes = [...] }` (Kademlia)
       - New: `[wavs.p2p.local]` with `listen_port`, `peer_addresses`, `authorized_peers` (lookup mode) or `[wavs.p2p.remote]` with `listen_port`, `bootstrappers`, `authorized_peers` (discovery mode)
       - New optional fields: `max_message_size` (default 65536), `deque_size` (default 128)

       ### 4. Discovery Mechanism Change
       - Old: mDNS for local, Kademlia DHT for production
       - New: Lookup mode (explicit peer addresses) for local, Discovery mode (bootstrapper-based) for production
       - No more automatic LAN discovery -- peers must be explicitly listed in Local mode

       ## Coordinated Upgrade Requirement
       **All operators in a deployment MUST upgrade simultaneously.**
       Old (libp2p) and new (commonware) nodes cannot communicate.
       Plan a maintenance window with all operators.

       ## Step-by-Step Migration

       ### 1. Coordinate with Other Operators
       [...]

       ### 2. Stop All Operator Nodes
       [...]

       ### 3. Update WAVS Binary
       [...]

       ### 4. Generate New Ed25519 Identity
       wavs-cli p2p identity --mnemonic "your signing mnemonic words"
       Share the new peer ID with other operators.

       ### 5. Update wavs.toml Configuration
       [Before/after examples for Local and Remote modes]

       ### 6. Exchange Peer Information
       Each operator shares their new Ed25519 public key and listen address.
       Format: <hex_pubkey>@<host>:<port>

       ### 7. Update authorized_peers
       Add all other operators' Ed25519 public keys to the authorized_peers list.

       ### 8. Start All Operator Nodes
       Start all nodes. Verify connectivity via GET /p2p/status.

       ## Verification
       - GET /p2p/status should show connected_peers matching expected count
       - peer_ids should list all other operators' Ed25519 public keys
       - subscribed_services should show registered services

       ## Rollback
       Rolling back requires ALL operators to revert simultaneously to the old binary.
       The old libp2p and new commonware backends are incompatible.

       ## Reference
       - [P2P.md](P2P.md) -- Full P2P configuration reference
       - [ARCHITECTURE.md](ARCHITECTURE.md) -- System architecture overview
       ```
  </action>
  <verify>
    <automated>cd /Users/jacobhartnell/Dev/WAVS/WAVS && test -f docs/blog/commonware-p2p-migration.md && test -f docs/OPERATOR_MIGRATION.md && grep -c "Ed25519" docs/OPERATOR_MIGRATION.md && grep -c "commonware" docs/blog/commonware-p2p-migration.md && echo "Docs created"</automated>
  </verify>
  <acceptance_criteria>
    - docs/blog/commonware-p2p-migration.md exists
    - docs/blog/commonware-p2p-migration.md contains "commonware" (at least 3 occurrences)
    - docs/blog/commonware-p2p-migration.md contains "## What Changed"
    - docs/blog/commonware-p2p-migration.md contains "## Impact on Operators"
    - docs/blog/commonware-p2p-migration.md does NOT contain step-by-step migration instructions (that's the migration guide)
    - docs/blog/commonware-p2p-migration.md has at least 40 lines
    - docs/OPERATOR_MIGRATION.md exists
    - docs/OPERATOR_MIGRATION.md contains "Ed25519" (at least 3 occurrences)
    - docs/OPERATOR_MIGRATION.md contains "## Breaking Changes"
    - docs/OPERATOR_MIGRATION.md contains "## Coordinated Upgrade Requirement"
    - docs/OPERATOR_MIGRATION.md contains "## Step-by-Step Migration"
    - docs/OPERATOR_MIGRATION.md contains "secp256k1" (documenting the old format)
    - docs/OPERATOR_MIGRATION.md contains "authorized_peers"
    - docs/OPERATOR_MIGRATION.md contains "wavs-cli p2p identity"
    - docs/OPERATOR_MIGRATION.md has at least 60 lines
  </acceptance_criteria>
  <done>Blog post announces commonware migration in announcement style, operator migration guide documents all breaking changes (identity, config, address format) with step-by-step instructions and coordinated upgrade requirement</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "commonware" docs/P2P.md` returns >= 5
2. `grep -c "GossipSub\|mDNS\|Kademlia" docs/P2P.md` returns 0
3. `grep -c "commonware" docs/ARCHITECTURE.md` returns >= 1
4. `grep "libp2p" CLAUDE.md` returns no matches
5. `test -f docs/blog/commonware-p2p-migration.md` exits 0
6. `test -f docs/OPERATOR_MIGRATION.md` exits 0
7. `wc -l docs/P2P.md` returns >= 80
8. `wc -l docs/OPERATOR_MIGRATION.md` returns >= 60
</verification>

<success_criteria>
- docs/P2P.md completely rewritten for commonware (no libp2p/GossipSub/mDNS/Kademlia references)
- docs/ARCHITECTURE.md P2P section updated
- CLAUDE.md no longer references libp2p
- Blog post exists in announcement style
- Operator migration guide covers identity change, config change, address change, and coordinated upgrade
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-and-cleanup/04-02-SUMMARY.md`
</output>
